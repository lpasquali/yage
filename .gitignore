# Built binary at repo root (do not ignore cmd/yage/ source dir).
/yage
/yage-bin
/bootstrap-capi
/bootstrap-capi-bin
/dist/
/tmp/
*.test
*.out
.env
bin

# Claude Code agent worktrees (transient — never commit)
.claude/

# Vendored upstream repos (clones, not submodules)
/cluster-api-provider-proxmox/
